import Links from "./links";

export default function Header() {
    return (
        <header className="header">
            <h2 className="logoname">Codevengers</h2>
            <Links />
        </header>
    );
}